/** Tailwind CSS v4 is a PostCSS plugin; there is no tailwind.config.js.
 *  Theme values live in src/app/globals.css under @theme. */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
